import { shallow } from 'enzyme';
import React from 'react';

import { DashboardView } from '../../../src/pages';

describe('DashboardView', () => {
  it('needs testing', () => {
    expect(1).toEqual(1);
  });
});